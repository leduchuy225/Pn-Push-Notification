import 'package:flutter/material.dart';

class MySelectorItem extends StatelessWidget {
  const MySelectorItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
